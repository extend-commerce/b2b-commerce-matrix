import './index.css';

export default function App() {
  return (
    <div style={{ background: 'var(--bg-base)', minHeight: '100vh', color: 'var(--text-primary)', padding: '2rem', fontFamily: 'Inter, sans-serif' }}>
      <h1>B2B Commerce Capability Planner — scaffold complete</h1>
    </div>
  );
}
